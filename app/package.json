{
  "name": "nb-channel-app",
  "version": "1.0.0",
  "private": true,
  "description": "NB频道 官网安卓客户端(Capacitor 套壳,加载线上站点,网站更新后 APP 自动同步)",
  "scripts": {
    "sync": "cap sync android",
    "build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
